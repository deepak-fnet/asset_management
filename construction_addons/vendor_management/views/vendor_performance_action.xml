<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="action_vendor_periodic_reviews" model="ir.actions.act_window">
        <field name="name">Periodic Reviews</field>
        <field name="res_model">vendor.performance</field>
        <field name="view_mode">list,form</field>
        <field name="context">{}</field>
    </record>

    <record id="action_vendor_reviews" model="ir.actions.act_window">
        <field name="name">Vendor Reviews</field>
        <field name="res_model">vendor.performance</field>
        <field name="view_mode">list,form</field>
    </record>

</odoo>
