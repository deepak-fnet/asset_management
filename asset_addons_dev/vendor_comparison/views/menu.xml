<?xml version="1.0" encoding="UTF-8"?>
<odoo>
      <record id="action_vendor_comparison" model="ir.actions.act_window">
        <field name="name">Vendor Comparison</field>
        <field name="res_model">vendor.comparison</field>
        <field name="view_mode">list,form</field>
        <field name="view_id" eval="False"/>
        <field name="target">current</field>
    </record>

    <!-- Menu Item -->
    <menuitem
        id="menu_vendor_comparison"
        name="Vendor Comparison"
        parent="purchase.menu_procurement_management"
        sequence="14"
        action="action_vendor_comparison"/>
</odoo>