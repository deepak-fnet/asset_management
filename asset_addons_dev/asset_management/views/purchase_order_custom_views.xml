<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- =========================================================
         CUSTOM PURCHASE ORDER — TREE
         ========================================================= -->
    <record id="view_asset_po_tree" model="ir.ui.view">
        <field name="name">asset.purchase.order.tree</field>
        <field name="model">purchase.order</field>
        <field name="priority">99</field>
        <field name="arch" type="xml">
            <list string="Asset Purchase Orders"
                  decoration-muted="state == 'cancel'"
                  decoration-success="state == 'done'"
                  decoration-info="state == 'draft'"
                  decoration-warning="state == 'sent'"
                  decoration-primary="state == 'purchase'">
                <field name="priority" widget="priority" optional="show" nolabel="1"/>
                <field name="name" string="Reference"/>
                <field name="partner_ref" optional="show"/>
                <field name="date_order"/>
                <field name="date_approve" optional="hide"/>
                <field name="asset_request_id" string="Asset Request" optional="show"/>
                <field name="partner_id"/>
                <field name="user_id" widget="many2one_avatar_user" optional="show"/>
                <field name="origin" optional="hide"/>
                <field name="company_id" groups="base.group_multi_company" optional="show"/>
                <field name="amount_untaxed" sum="Total Untaxed Amount" widget="monetary" optional="hide"/>
                <field name="amount_total" sum="Total" widget="monetary"/>
                <field name="currency_id" column_invisible="True"/>
                <field name="invoice_status" widget="badge"
                       decoration-success="invoice_status == 'invoiced'"
                       decoration-info="invoice_status == 'to invoice'"
                       decoration-warning="invoice_status == 'no'" optional="show"/>
                <field name="state" widget="badge"
                       decoration-info="state == 'draft'"
                       decoration-warning="state == 'sent'"
                       decoration-primary="state == 'purchase'"
                       decoration-success="state == 'done'"
                       decoration-muted="state == 'cancel'"/>
            </list>
        </field>
    </record>

    <!-- =========================================================
         CUSTOM PURCHASE ORDER — FORM
         Full clone of base purchase.order form
         + purchase_stock additions (Receive button, Receipt smart
           button, picking_type_id, dest_address_id, incoterm, etc.)
         + our 2 custom additions (asset_request smart button +
           Asset Inventory page).
         ========================================================= -->
    <record id="view_asset_po_form" model="ir.ui.view">
        <field name="name">asset.purchase.order.form</field>
        <field name="model">purchase.order</field>
        <field name="priority">99</field>
        <field name="arch" type="xml">
            <form string="Asset Purchase Order" class="o_purchase_order">
                <header>
                    <button name="action_rfq_send" invisible="state != 'draft'"
                            string="Send by Email" type="object"
                            context="{'send_rfq':True}"
                            class="oe_highlight" data-hotkey="g"/>
                    <button name="print_quotation" string="Print RFQ" type="object"
                            invisible="state != 'draft'"
                            class="oe_highlight" groups="base.group_user" data-hotkey="k"/>
                    <button name="button_confirm" type="object"
                            invisible="state != 'sent'"
                            string="Confirm Order"
                            context="{'validate_analytic': True}"
                            class="oe_highlight" id="bid_confirm" data-hotkey="q"/>
                    <button name="button_approve" type="object"
                            invisible="state != 'to approve'"
                            string="Approve Order" class="oe_highlight"
                            groups="purchase.group_purchase_manager" data-hotkey="z"/>

                    <!-- purchase_stock: Receive Products button -->
                    <button name="action_view_picking"
                            string="Receive Products" type="object"
                            class="oe_highlight"
                            invisible="is_shipped or state not in ('purchase', 'done') or incoming_picking_count == 0"
                            data-hotkey="y"
                            groups="stock.group_stock_user"/>

                    <!-- purchase_stock: Confirm Receipt Date -->
                    <button name="confirm_reminder_mail" string="Confirm Receipt Date" type="object"
                            invisible="state not in ('purchase', 'done') or mail_reminder_confirmed or not date_planned or effective_date"
                            groups="base.group_no_one" data-hotkey="o"/>

                    <button name="action_create_invoice" string="Create Bill" type="object"
                            class="oe_highlight" context="{'create_bill':True}"
                            invisible="state not in ('purchase', 'done') or invoice_status in ('no', 'invoiced')"
                            data-hotkey="w"/>
                    <button name="action_rfq_send" invisible="state != 'sent'"
                            string="Re-Send by Email" type="object"
                            context="{'send_rfq':True}" data-hotkey="g"/>
                    <button name="print_quotation" string="Print RFQ" type="object"
                            invisible="state != 'sent'"
                            groups="base.group_user" data-hotkey="k"/>
                    <button name="button_confirm" type="object"
                            invisible="state != 'draft'"
                            context="{'validate_analytic': True}"
                            string="Confirm Order" id="draft_confirm" data-hotkey="q"/>
                    <button name="action_rfq_send" invisible="state != 'purchase'"
                            string="Send PO by Email" type="object"
                            context="{'send_rfq':False}" data-hotkey="g"/>
                    <button name="action_create_invoice" string="Create Bill" type="object"
                            context="{'create_bill':True}"
                            invisible="state not in ('purchase', 'done') or invoice_status not in ('no', 'invoiced') or not order_line"
                            data-hotkey="w"/>
                    <button name="button_draft" invisible="state != 'cancel'"
                            string="Set to Draft" type="object" data-hotkey="o"/>
                    <button name="button_cancel"
                            invisible="state not in ('draft', 'to approve', 'sent', 'purchase')"
                            string="Cancel" type="object" data-hotkey="x"/>
                    <button name="button_done" type="object" string="Lock"
                            invisible="state != 'purchase'" data-hotkey="l"/>
                    <button name="button_unlock" type="object" string="Unlock"
                            invisible="state != 'done'"
                            groups="purchase.group_purchase_manager" data-hotkey="l"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,sent,purchase" readonly="1"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <!-- purchase_stock: Receipt smart button -->
                        <button type="object" name="action_view_picking"
                                class="oe_stat_button" icon="fa-truck"
                                invisible="incoming_picking_count == 0"
                                groups="stock.group_stock_user">
                            <field name="incoming_picking_count" widget="statinfo"
                                   string="Receipt"
                                   help="Incoming Shipments"/>
                        </button>
                        <!-- Vendor Bills smart button -->
                        <button type="object" name="action_view_invoice"
                                class="oe_stat_button" icon="fa-pencil-square-o"
                                invisible="invoice_count == 0 or state in ('draft', 'sent', 'to approve')">
                            <field name="invoice_count" widget="statinfo" string="Vendor Bills"/>
                            <field name="invoice_ids" invisible="1"/>
                        </button>
                        <!-- Our custom Asset Request smart button -->
                        <button name="action_open_asset_request"
                                type="object"
                                class="oe_stat_button"
                                icon="fa-file-text-o"
                                invisible="not asset_request_id">
                            <div class="o_stat_info">
                                <span class="o_stat_text">Asset</span>
                                <span class="o_stat_text">Request</span>
                            </div>
                        </button>
                    </div>

                    <div class="oe_title">
                        <span class="o_form_label" invisible="state not in ('draft', 'sent')">Request for Quotation </span>
                        <span class="o_form_label" invisible="state in ('draft', 'sent')">Purchase Order </span>
                        <h1 class="d-flex">
                            <field name="priority" widget="priority" class="me-3"/>
                            <field name="name" readonly="1"/>
                        </h1>
                    </div>

                    <group>
                        <group>
                            <field name="partner_id"
                                   widget="res_partner_many2one"
                                   context="{'res_partner_search_mode': 'supplier', 'show_vat': True}"
                                   placeholder="Name, TIN, Email, or Reference"
                                   options="{'no_create': True}"
                                   readonly="state in ['cancel', 'done', 'purchase']"/>
                            <field name="partner_ref"/>
                            <field name="currency_id" groups="base.group_multi_currency"
                                   force_save="1"
                                   readonly="state in ['cancel', 'done', 'purchase']"/>
                            <field name="id" invisible="1"/>
                            <field name="company_id" invisible="1"
                                   readonly="state in ['cancel', 'done', 'purchase']"/>
                            <field name="currency_id" invisible="1"
                                   readonly="state in ['cancel', 'done', 'purchase']"
                                   groups="!base.group_multi_currency"/>
                            <field name="tax_calculation_rounding_method" invisible="1"/>

                            <!-- purchase_stock: is_shipped — referenced by Receive button's invisible -->
                            <field name="is_shipped" invisible="1"/>

                            <!-- our links -->
                            <field name="asset_request_id" readonly="1"
                                   invisible="not asset_request_id"/>
                            <field name="asset_list_count" invisible="1"/>
                        </group>
                        <group>
                            <field name="date_order"
                                   invisible="state in ('purchase', 'done')"
                                   readonly="state in ['cancel', 'done', 'purchase']"/>
                            <label for="date_approve" invisible="state not in ('purchase', 'done')"/>
                            <div name="date_approve" invisible="state not in ('purchase', 'done')" class="o_row">
                                <field name="date_approve"/>
                                <field name="mail_reception_confirmed" invisible="1"/>
                                <span class="text-muted" invisible="not mail_reception_confirmed">(confirmed by vendor)</span>
                            </div>
                            <label for="date_planned"/>
                            <div name="date_planned_div" class="o_row">
                                <field name="date_planned" string="Expected Arrival"
                                       readonly="state not in ('draft', 'sent', 'to approve', 'purchase')"/>
                                <field name="mail_reminder_confirmed" invisible="1"/>
                                <span class="text-muted" invisible="not mail_reminder_confirmed">(confirmed by vendor)</span>
                            </div>
                            <label for="receipt_reminder_email" class="d-none"
                                   groups="purchase.group_send_reminder"
                                   invisible="effective_date"/>
                            <div name="reminder" class="o_row"
                                 groups="purchase.group_send_reminder"
                                 invisible="effective_date"
                                 title="Automatically send a confirmation email to the vendor X days before the expected receipt date.">
                                <field name="receipt_reminder_email"/>
                                <span>Ask confirmation</span>
                                <div class="o_row oe_inline" invisible="not receipt_reminder_email">
                                    <field name="reminder_date_before_receipt"/>
                                    day(s) before
                                </div>
                            </div>
                            <field name="effective_date" invisible="not effective_date"/>

                            <!-- purchase_stock: Deliver To + drop-ship address -->
                            <field name="picking_type_id"
                                   string="Deliver To"
                                   domain="[('code','=','incoming'), '|', ('warehouse_id', '=', False), ('warehouse_id.company_id', '=', company_id)]"
                                   options="{'no_create': True}"
                                   groups="stock.group_stock_multi_locations"
                                   readonly="state in ['cancel', 'done', 'purchase']"/>
                            <field name="default_location_dest_id_usage" invisible="1"/>
                            <field name="dest_address_id"
                                   groups="stock.group_stock_multi_locations"
                                   invisible="default_location_dest_id_usage != 'customer'"
                                   readonly="state in ['cancel', 'done', 'purchase']"
                                   required="default_location_dest_id_usage == 'customer'"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Products" name="products">
                            <field name="tax_country_id" invisible="1"/>
                            <field name="order_line" widget="section_and_note_one2many"
                                   mode="tree,kanban"
                                   context="{'default_state': 'draft'}"
                                   readonly="state in ('done', 'cancel')">
                                <list string="Purchase Order Lines" editable="bottom">
                                    <control>
                                        <create name="add_product_control" string="Add a product"/>
                                        <create name="add_section_control" string="Add a section"
                                                context="{'default_display_type': 'line_section'}"/>
                                        <create name="add_note_control" string="Add a note"
                                                context="{'default_display_type': 'line_note'}"/>
                                        <button name="action_add_from_catalog" string="Catalog"
                                                type="object" class="px-4 btn-link"
                                                context="{'order_id': parent.id}"/>
                                    </control>
                                    <field name="tax_calculation_rounding_method" column_invisible="True"/>
                                    <field name="display_type" column_invisible="True"/>
                                    <field name="company_id" column_invisible="True"/>
                                    <field name="currency_id" column_invisible="True"/>
                                    <field name="state" column_invisible="True"/>
                                    <field name="product_type" column_invisible="True"/>
                                    <field name="product_uom" column_invisible="True" groups="!uom.group_uom"/>
                                    <field name="product_uom_category_id" column_invisible="True"/>
                                    <field name="invoice_lines" column_invisible="True"/>
                                    <!-- purchase_stock: move_dest_ids — needed internally -->
                                    <field name="move_dest_ids" column_invisible="True"/>
                                    <field name="sequence" widget="handle"/>
                                    <field name="product_id"
                                           readonly="state in ('done', 'cancel')"
                                           required="not display_type"
                                           width="35%"
                                           context="{'partner_id': parent.partner_id, 'quantity': product_qty, 'company_id': parent.company_id, 'use_partner_name': False}"
                                           force_save="1"
                                           domain="[('purchase_ok', '=', True)]"/>
                                    <field name="name" widget="section_and_note_text"/>
                                    <field name="date_planned" optional="hide"
                                           required="not display_type" force_save="1"/>
                                    <!-- purchase_stock: forecasted_issue + forecast report button -->
                                    <field name="forecasted_issue" column_invisible="True"/>
                                    <field name="analytic_distribution"
                                           widget="analytic_distribution"
                                           optional="hide"
                                           groups="analytic.group_analytic_accounting"
                                           options="{'product_field': 'product_id', 'business_domain': 'purchase_order', 'amount_field': 'price_subtotal'}"/>
                                    <field name="product_qty"/>
                                    <button type="object" name="action_product_forecast_report"
                                            title="Forecast Report" icon="fa-area-chart"
                                            invisible="not id or not forecasted_issue or product_type != 'product'"
                                            class="text-danger"/>
                                    <button type="object" name="action_product_forecast_report"
                                            title="Forecast Report" icon="fa-area-chart"
                                            invisible="not id or forecasted_issue or product_type != 'product'"/>
                                    <field name="qty_received_manual" column_invisible="True"/>
                                    <field name="qty_received_method" column_invisible="True"/>
                                    <field name="qty_received" string="Received"
                                           column_invisible="parent.state not in ('purchase', 'done')"
                                           readonly="product_type in ('consu', 'product')"
                                           optional="show"/>
                                    <field name="qty_invoiced" string="Billed"
                                           column_invisible="parent.state not in ('purchase', 'done')"
                                           optional="show"/>
                                    <field name="product_uom" string="UoM"
                                           groups="uom.group_uom"
                                           readonly="state in ('done', 'cancel')"
                                           required="not display_type"
                                           options="{'no_open': True}"
                                           force_save="1" optional="show"/>
                                    <field name="product_packaging_qty"
                                           invisible="not product_id or not product_packaging_id"
                                           groups="product.group_stock_packaging" optional="show"/>
                                    <field name="product_packaging_id"
                                           invisible="not product_id"
                                           context="{'default_product_id': product_id, 'list_view_ref':'product.product_packaging_tree_view', 'form_view_ref':'product.product_packaging_form_view'}"
                                           groups="product.group_stock_packaging" optional="show"/>
                                    <field name="price_unit" readonly="qty_invoiced != 0"/>
                                    <button name="action_purchase_history" type="object"
                                            icon="fa-history" title="Purchase History"
                                            invisible="not id"/>
                                    <field name="taxes_id" widget="many2many_tags"
                                           domain="[('type_tax_use', '=', 'purchase'), ('company_id', 'parent_of', parent.company_id), ('country_id', '=', parent.tax_country_id), ('active', '=', True)]"
                                           context="{'default_type_tax_use': 'purchase', 'search_view_ref': 'account.account_tax_view_search'}"
                                           options="{'no_create': True}" optional="show"/>
                                    <field name="discount" string="Disc.%"
                                           readonly="qty_invoiced != 0" optional="hide"/>
                                    <field name="price_subtotal" string="Tax excl."/>
                                    <field name="price_total" string="Tax incl."
                                           column_invisible="parent.tax_calculation_rounding_method == 'round_globally'"
                                           optional="hide"/>
                                </list>
                            </field>
                            <group>
                                <group>
                                    <field colspan="2" name="notes" nolabel="1"
                                           placeholder="Define your terms and conditions ..."/>
                                </group>
                                <group class="oe_subtotal_footer">
                                    <field name="tax_totals" widget="account-tax-totals-field"
                                           nolabel="1" colspan="2" readonly="1"/>
                                </group>
                            </group>
                            <div class="clearfix"/>
                        </page>

                        <page string="Other Information" name="purchase_delivery_invoice">
                            <group>
                                <group name="other_info">
                                    <field name="user_id" domain="[('share', '=', False)]"
                                           widget="many2one_avatar_user"/>
                                    <field name="company_id" groups="base.group_multi_company"
                                           options="{'no_create': True}"
                                           readonly="state in ['cancel', 'done', 'purchase']"/>
                                    <field name="origin"/>
                                    <!-- purchase_stock: incoterm fields -->
                                    <field name="incoterm_id" readonly="state == 'done'"/>
                                    <field name="incoterm_location" readonly="state == 'done'"/>
                                </group>
                                <group name="invoice_info">
                                    <field name="invoice_status"
                                           invisible="state in ('draft', 'sent', 'to approve', 'cancel')"/>
                                    <!-- purchase_stock: receipt_status -->
                                    <field name="receipt_status"
                                           invisible="state not in ('purchase', 'done')"/>
                                    <field name="payment_term_id"
                                           readonly="invoice_status == 'invoiced' or state == 'done'"
                                           options="{'no_create': True}"/>
                                    <field name="fiscal_position_id"
                                           options="{'no_create': True}"
                                           readonly="invoice_status == 'invoiced' or state == 'done'"/>
                                </group>
                            </group>
                        </page>

                        <page string="Asset Inventory" name="asset_inventory"
                              invisible="asset_list_count == 0">
                            <field name="asset_list_ids" readonly="1">
                                <list>
                                    <field name="serial_no"/>
                                    <field name="product_id"/>
                                    <field name="asset_id"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <!-- =========================================================
         SEARCH VIEW
         ========================================================= -->
    <record id="view_asset_po_search" model="ir.ui.view">
        <field name="name">asset.purchase.order.search</field>
        <field name="model">purchase.order</field>
        <field name="priority">99</field>
        <field name="arch" type="xml">
            <search>
                <field name="name" string="Purchase Order"
                       filter_domain="['|','|',('name','ilike',self),('partner_ref','ilike',self),('partner_id','child_of',self)]"/>
                <field name="partner_id" operator="child_of"/>
                <field name="asset_request_id"/>
                <field name="product_id"/>
                <field name="user_id"/>
                <filter string="From Asset Request" name="from_asset_request"
                        domain="[('asset_request_id', '!=', False)]"/>
                <separator/>
                <filter string="My Orders" name="my_orders" domain="[('user_id','=',uid)]"/>
                <separator/>
                <filter string="Draft RFQ" name="po_draft" domain="[('state', '=', 'draft')]"/>
                <filter string="RFQ Sent" name="po_sent" domain="[('state', '=', 'sent')]"/>
                <filter string="Purchase Orders" name="po_purchase" domain="[('state', '=', 'purchase')]"/>
                <filter string="Done" name="po_done" domain="[('state', '=', 'done')]"/>
                <filter string="Cancelled" name="po_cancel" domain="[('state', '=', 'cancel')]"/>
                <separator/>
                <filter string="Order Date" name="filter_date_order" date="date_order"/>
<!--                <group expand="0" string="Group By">-->
<!--                    <filter string="Vendor" name="po_group_partner" context="{'group_by': 'partner_id'}"/>-->
<!--                    <filter string="Asset Request" name="po_group_request" context="{'group_by': 'asset_request_id'}"/>-->
<!--                    <filter string="Purchase Representative" name="po_group_user" context="{'group_by': 'user_id'}"/>-->
<!--                    <filter string="Status" name="po_group_state" context="{'group_by': 'state'}"/>-->
<!--                    <filter string="Order Date" name="po_group_date_order" context="{'group_by': 'date_order'}"/>-->
<!--                </group>-->
            </search>
        </field>
    </record>

    <!-- =========================================================
         ACTION
         ========================================================= -->
    <record id="action_asset_purchase_order" model="ir.actions.act_window">
        <field name="name">Asset Purchase Orders</field>
        <field name="res_model">purchase.order</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('asset_request_id', '!=', False)]</field>
        <field name="search_view_id" ref="view_asset_po_search"/>
        <field name="context">{
            'list_view_ref': 'asset_management.view_asset_po_tree',
            'form_view_ref': 'asset_management.view_asset_po_form',
            'search_default_from_asset_request': 1,
        }</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No asset Purchase Orders yet
            </p>
            <p>
                Purchase Orders created from approved Asset Requests will show up here.
            </p>
        </field>
    </record>

    <record id="action_asset_po_tree_view" model="ir.actions.act_window.view">
        <field name="sequence" eval="1"/>
        <field name="view_mode">list</field>
        <field name="view_id" ref="view_asset_po_tree"/>
        <field name="act_window_id" ref="action_asset_purchase_order"/>
    </record>
    <record id="action_asset_po_form_view" model="ir.actions.act_window.view">
        <field name="sequence" eval="2"/>
        <field name="view_mode">form</field>
        <field name="view_id" ref="view_asset_po_form"/>
        <field name="act_window_id" ref="action_asset_purchase_order"/>
    </record>

</odoo>